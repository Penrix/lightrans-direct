/**
 * style for notification items
 */
final-opacity = 0.95;
animation-duration = 400ms;

.lightrans-notifier-item {
	display: flex;
	flex-direction: row;
	justify-content: center;
	align-items: flex-start;
	max-width: 25vw;
	padding: 1.5vh 1vw 1.5vh 1vw;
	margin: 1.5vh 0;
	pointer-events: all;
	border-radius: 4px;
	background-color: white;
	box-shadow: 0 6px 16px 0 rgba(0, 0, 0, 0.08);
	opacity: final-opacity;
}

.lightrans-notifier-success {
	background-color: #f6ffed;
	border: 1px solid #b7eb8f;
}

.lightrans-notifier-info {
	background-color: #e6f7ff;
	border: 1px solid #91d5ff;
}

.lightrans-notifier-warning {
	background-color: #fffbe6;
	border: 1px solid #ffe58f;
}

.lightrans-notifier-error {
	background-color: #fff2f0;
	border: 1px solid #ffccc7;
}

.lightrans-notifier-icon {
	font-size: large;
	margin-right: 15px;
	position: relative;
	top: 3px;
}

.lightrans-notifier-content {
	height: 100%;
	display: flex;
	flex-direction: column;
	justify-content: flex-start;
	align-items: flex-start;
}

.lightrans-notifier-title {
	font-size: 16px;
}

.lightrans-notifier-detail {
	font-size: 14px;
	padding-top: 10px;
}

.lightrans-notifier-close {
	cursor: pointer;
	font-size: x-small;
	margin-left: 15px;
	position: relative;
	top: 6px;
	fill: rgba(0, 0, 0, 0.45);

	:hover {
		fill: rgba(0, 0, 0, 0.9);
	}
}

.lightrans-notifier-show-animation {
	animation-name: lightrans-notifier-show-animation;
	animation-duration: animation-duration;
	animation-timing-function: cubic-bezier(0.45, 1.45, 0.75, 1);
}

.lightrans-notifier-disappear-animation {
	animation-name: lightrans-notifier-disappear-animation;
	animation-duration: animation-duration;
	animation-timing-function: ease;
}

@keyframes lightrans-notifier-show-animation {
	from {
		transform: translate(0, -30px) scale(0.75);
		opacity: 0;
	}

	to {
		transform: translate(0, 0) scale(1);
		opacity: final-opacity;
	}
}

@keyframes lightrans-notifier-disappear-animation {
	from {
		transform: translate(0, 0) scale(1);
		opacity: final-opacity;
	}

	to {
		transform: translate(0, -30px) scale(0.75);
		opacity: 0;
	}
}